```
    // let prev=result;
    if(x==='start'){
      setInterval(()=>{
        setresult(prev=>prev+1);
      },1000)
```

## here react is handing over the val of result to prev even if we dont assign it  
  
